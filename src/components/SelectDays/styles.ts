import styled from 'styled-components'
import { color } from '../../theme'

export const SelectDaysStyle = styled.select`
  height: 30px;
  padding: 0 1.5rem;

  .timeSet {
    font-weight: 600;
    color: ${color.blue};
  }
`
